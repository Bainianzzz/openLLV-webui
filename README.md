# openLLV WebUI

基于 Gradio 的 openLLV 图像增强与模型训练界面。

## 快速开始

```bash
# 1. 安装依赖（Python 环境与依赖由 uv 统一管理）
uv sync

# 2. 准备配置文件
cp example.config.yaml config.yaml

# 3. 启动界面
uv run python app.py
# or
pnpm run dev
```
